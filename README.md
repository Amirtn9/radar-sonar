# رادار سونار

**Radar Sonar** یک ربات تلگرام قدرتمند برای مانیتورینگ سرورها است که به شما امکان می‌دهد سرورها را مدیریت کنید، آمار و عملکرد آنها را مشاهده کنید، فرمان‌های راه دور اجرا کنید و وضعیت شبکه را از طریق رابط کاربری آسان تلگرام پیگیری کنید.

---

## ویژگی‌ها

* ✅ افزودن و مدیریت چندین سرور
* 📊 مشاهده آمار سرورها به صورت زنده (CPU, RAM, Disk, Uptime, Traffic)
* 🌍 مانیتورینگ پینگ و تأخیر جهانی (ادغام با Check-Host)
* 🔄 اجرای فرمان‌های راه دور از طریق SSH
* 🛠 مدیریت Speedtest و DNS روی سرورها
* 🧹 پاکسازی RAM سرورها از راه دور
* 📦 بروزرسانی کامل سیستم و مخازن
* 👑 پنل مدیریت برای کاربران و سرورها
* 📢 ارسال پیام همگانی به کاربران
* 🗂 سازماندهی سرورها در گروه‌ها
* 🔐 کنترل دسترسی کاربران و مدیریت اشتراک
* 🖥 نمودارهای گرافیکی عملکرد سرورها در ۲۴ ساعت گذشته

---

## نصب

برای نصب رادار سونار کافی است دستور زیر را در ترمینال خود اجرا کنید:

```bash
bash <(curl -s https://raw.githubusercontent.com/Amirtn9/radar-sonar/main/install.sh)
```

این دستور اسکریپت نصب را اجرا می‌کند، اطلاعات مورد نیاز را از شما می‌گیرد و ربات را راه‌اندازی می‌کند.

---

## استفاده

* ربات را با ارسال دستور `/start` به تلگرام شروع کنید.
* با استفاده از کیبورد درون‌خطی، سرورها را اضافه و مدیریت کنید، داشبورد را مشاهده کنید.
* کاربران ادمین به قابلیت‌های اضافی مانند مدیریت کاربران، بکاپ‌ها و پیام همگانی دسترسی دارند.
* وضعیت سرورها و آمار عملکرد به صورت زنده بروزرسانی می‌شود.

---

## پیکربندی

* **TOKEN**: توکن ربات تلگرام شما.
* **SUPER_ADMIN_ID**: آیدی تلگرام مدیر ربات.
* **DB_NAME**: نام فایل دیتابیس SQLite.
* **KEY_FILE**: فایل نگهداری کلید رمزگذاری برای رمزهای عبور سرورها.

---

## وابستگی‌ها

* Python 3.10+
* `python-telegram-bot` برای اتصال به تلگرام
* `paramiko` برای اتصال SSH
* `cryptography` برای رمزگذاری پسوردها
* `requests` برای درخواست‌های HTTP
* `matplotlib` برای ایجاد نمودارها
* `jdatetime` برای قالب‌بندی تاریخ فارسی

تمام وابستگی‌ها به صورت خودکار توسط اسکریپت نصب مدیریت می‌شوند.

---

## پایگاه داده

Radar Sonar از **SQLite** برای ذخیره موارد زیر استفاده می‌کند:

* اطلاعات کاربران و اشتراک‌ها
* سرورها و گروه‌ها
* کانال‌ها برای اطلاع‌رسانی
* آمار سرورها (CPU, RAM)
* تنظیمات ربات

دیتابیس به صورت خودکار در اولین اجرا ایجاد می‌شود.

---

## مجوز

این پروژه متن‌باز است و به صورت عمومی در [GitHub](https://github.com/Amirtn9/Radar-Sonar) موجود است. می‌توانید آن را اصلاح و استفاده کنید.
